<div class="action-bar">
  <h2>Tags for <span style="color: #c00"><%= @project.name %></span></h2>
  <p class="subtitle">You've created <%= pluralize(@tags.length, 'tags') %> for this project.</p>
</div>

<%= tag_list(@project) %>
