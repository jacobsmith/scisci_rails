<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "SciSci Notes" %></title>

    <link href='http://fonts.googleapis.com/css?family=Open+Sans:300,500,400,600,700,400italic' rel='stylesheet' type='text/css'>

    <%= stylesheet_link_tag("application") %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-45184697-2', 'sciscinotes.com');
      ga('send', 'pageview');

    </script>
  </head>

  <body>
    <%= render 'layouts/top_bar' %>

    <!-- Class header -->
    <% if @section %>
      <div class="class-header">
        <div class="row">
          <div class="small-12 columns">
            <p class="class-header-title"><%= @section.name %></p>
          </div>
        </div>
      </div>
    <% end %> 
    <!-- /Class header -->

  <%= render 'layouts/thesis' %>

    <!-- Project nav -->
    <div class="project-nav">
      <div class="row">
        <div class="small-12 columns">
          <h1 class="project-nav-title"><%= project_title.to_s %></h1>
          <ul class="project-nav-menu">
            <%= render 'header/nav_menu' %>
          </ul>
        </div>
      </div>
    </div>
    <!-- /Project nav -->

    <div class="row main" role="main">
      <div class="large-12 columns">
        <%= render 'layouts/flash_error_messages' %>

        <%= yield %>
        <%= javascript_include_tag "application" %>
        <%= yield :javascript %>

        <%= debug(params) if Rails.env.development? || Rails.env == "development_postgres" %>

      </div>
    </div>

  </body>
</html>
