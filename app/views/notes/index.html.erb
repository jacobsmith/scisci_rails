<% @source = Source.find(params[:source_id]) %>
<% @notes = @source.notes %>

<h1 class="phrase-title"></h1>

<!-- <table>
  <thead>
    <tr>
      <th>Quote</th>
      <th>Comments</th>
      <th>Tags</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr>
        <td><%= note.quote %></td>
        <td><%= note.comments %></td>
        <td><%= tags_list(note) if note.tags != [] %></td>
        <td><%= link_to 'Show', note_path(note) %></td>
        <td><%= link_to 'Edit', edit_note_path(note) %></td>
        <td><%= link_to 'Destroy', note_path(note), method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<ul class="note-list">
  <% @notes.each do |note| %>
    <li class="note">
      <p class="note__comment"><%= note.comments %></p>
      <p class="note__quote"><%= note.quote %></p>
      <p class="note__source">Source: <a href="#">The Visual Display of Quantitative Information</a></p>
    </li>
  <% end %>
</ul>


<br>

<%= link_to 'New Note', new_source_note_path(@source) %> | 
<%= link_to 'Back to Source', source_path(@source) %>
