<h1>All Classes</h1>

<table>

  <tbody>
    <% @sections.each do |section| %>
        <tr>
          <td><%= link_to section.name, section_path(section)  %></td>
        </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Section', new_teacher_section_path(@sections.first.teacher_id),
  class: "button radius large-offset-1" if current_user.is_a? Teacher %>
