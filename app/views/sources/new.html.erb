<% if @source.project.inactive? %>
  <%= render 'projects/inactive_notice' %>
<% else %>
  <%= render "layouts/project_navigation" %>

  <h1>New source</h1>

  <%= render 'form' %>

  <%= link_to 'Back', projects_path %>
<% end %>
